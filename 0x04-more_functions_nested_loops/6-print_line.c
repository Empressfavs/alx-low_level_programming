#include "main.h"
/**
 * print_line -  A function that prits a straight line
 * @n: number of times character should be printed
 * Return: if n is 0 or less,, the function should only print new line 
 */
void print_line(int n)
{
	int a = 1;
	
	if (a >= 1)
	{
		_putchar("______");
			a == n;
	}
	else
	{
		_putchar('\n');
	}
	_putchar('\n');
}
